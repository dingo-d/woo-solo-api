#!/usr/bin/env php
<?php

use Symfony\Component\Console\Application;
use MadeByDenis\Commands\CleanCodeCoverage;

if (!in_array(PHP_SAPI, ['cli', 'phpdbg', 'embed'], true)) {
    error_log(sprintf('Warning: The console should be invoked via the CLI version of PHP, not the %s SAPI', PHP_SAPI));
}

set_time_limit(0);

require dirname(__DIR__).'/vendor/autoload.php';

$app = new Application();
$app->add(new CleanCodeCoverage());
$app->run();
